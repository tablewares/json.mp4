{
  "assetType": "SolidShape",
  "component": "SolidShape.jsx",
  "description": "A flat rectangle, pill, or circle that scales/fades in. The base primitive for background panels, chips, underlines, and color blocks — no text, no image, just fill.",
  "defaultSize": { "width": 400, "height": 120 },
  "defaultStyle": {
    "easing": "snappySpring",
    "shape": "rect",
    "backgroundColorToken": "accentBg",
    "borderRadius": 16,
    "revealMode": "scale"
  },
  "contentOverrideSchema": {
    "type": "object",
    "properties": {}
  },
  "styleOverrideSchema": {
    "type": "object",
    "properties": {
      "easing": { "type": "string" },
      "shape": { "type": "string", "enum": ["rect", "pill", "circle"] },
      "backgroundColorToken": { "type": "string" },
      "borderRadius": { "type": "number" },
      "revealMode": { "type": "string", "enum": ["scale", "fade", "wipe"] },
      "opacity": { "type": "number" },
      "gradient": {
        "type": "object",
        "description": "Optional linear gradient fill (raw hex literals). Takes priority over backgroundColor/backgroundColorToken.",
        "properties": {
          "from": { "type": "string" },
          "to": { "type": "string" },
          "angle": { "type": "number" }
        }
      },
      "width": { "type": "number" },
      "height": { "type": "number" }
    }
  }
}
