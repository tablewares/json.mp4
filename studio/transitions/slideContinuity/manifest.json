{
  "transitionType": "slideContinuity",
  "component": "SlideContinuity.jsx",
  "description": "Carries a named asset's resolved color + position across the cut so it visually 'becomes' the next scene's version of itself, instead of cutting and re-entering.",
  "defaultDurationInFrames": 24,
  "consumes": {
    "outgoingSceneStyles": true,
    "incomingSceneStyles": true,
    "carriedAssets": true
  },
  "params": {
    "carryAssetId": {
      "type": "string",
      "description": "asset id (must exist in both outgoing and incoming scene) to morph across the cut"
    }
  }
}
